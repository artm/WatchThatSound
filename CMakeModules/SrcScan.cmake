MACRO(SCAN_GLOB VAR GLOB DOCSTR)
  IF(NOT ${VAR} OR SRC_SCAN)
    FILE(GLOB _glob ${GLOB})
    SET(${VAR} ${_glob} CACHE INTERNAL ${DOCSTR} FORCE)
    MESSAGE("${DOCSTR}: ${${VAR}}")
  ENDIF()
ENDMACRO()

MACRO(SCAN_DONE)
  SET(SRC_SCAN 0 CACHE BOOL "Scan for sources" FORCE)
ENDMACRO()
